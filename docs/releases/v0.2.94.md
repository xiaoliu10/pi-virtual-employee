## 0.2.94

### 新特性

- **browser_hover 悬停工具**：无按键的纯鼠标移动，补齐了坐标输入原语的最后一块（此前点击/拖拽/滚动俱全，唯独没有悬停）。用于触发悬停效果——工具提示、悬浮菜单、hover 高亮；canvas 远程会话同样适用（webclient 会把 mousemove 翻译成远程指针移动）。现场用例：堡垒机 webclient 的云区切换菜单只在悬停时弹出。移动采用起点偏移 + 步进轨迹进入目标（真实 enter 过渡），默认停留 300ms 等菜单渲染，可调 `dwellMs`。
