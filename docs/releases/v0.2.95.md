## 0.2.95

### 修复

- **上下文超限不再中途砍掉长任务**：回合中预算门槛触发恢复时，压缩总结调用此前没有任何重试——中继一次瞬时抖动就宣告「自动压缩也没能腾出足够空间」并结束回合（现场 2026-09-23：堡垒机验证码 OCR 长任务被拦腰砍断）。现给总结调用加上有界重试（2 次，1s/2s 退避；确定性错误仍快速失败）。同时修复兜底裁剪 `truncateToFit` 对单轮转录的结构性失效：投放目标封顶为转录一半，且找不到用户消息边界时在原切点落刀、任务语句原样保留在接缝处——即使重试全败，也能确定性砍半续跑而不是放弃。恢复投放目标同步对齐到预算线之内（可用窗口的一半），砍完必过预算复查。
