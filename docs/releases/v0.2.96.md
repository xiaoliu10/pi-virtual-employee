## 0.2.96

### 修复

- **「压缩成功后又爆」的恢复死角**（现场 2026-09-24：SLS 长任务在 06:03 压缩成功把上下文从 158K 压到 ~18K 并继续任务，46 秒后新工具结果灌回 ~140K，第二次恢复却宣告「腾不出空间」终结回合）：第二次恢复的常规切点回退到「只覆盖上一次压缩摘要」的位置（cut=1），摘要后无内容可压（empty_head）直接放弃；而单轮强制切点（findForcedCompactionCut，能把新内容链到旧摘要上真正压缩）的触发条件只认「完全找不到切点」，漏掉了这种「切了等于没切」的情况。现补上触发条件——切点 ≤1 且转录头部是压缩摘要时同样走强制切点，恢复可以找到更深的切点、把新增内容摘要进链式总结，真正腾出空间。
- **强制压缩跳过原因落日志**：此前 empty_head / nothing_to_cut 静默返回，排查只能靠猜。现在强制压缩被跳过时记录原因与当时体量。
- **日志行修正**：「compacted in-turn」行的比较符此前固定打 `≥`，但走到该行的实际条件是恢复后**低于**预算（`<`），误导排查。已改为如实打印 `< … after recovery`。
